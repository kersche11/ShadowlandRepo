using UnityEngine;

public class GameManager : MonoBehaviour
{

    int bossFightSequence;
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
